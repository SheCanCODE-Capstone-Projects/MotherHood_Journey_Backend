package com.motherhood.journey.identity.enums;

/**
 * Alias for UserRole — kept for backward compatibility.
 * Prefer using {@link UserRole} directly.
 */
public enum RoleType {
    PATIENT,
    HEALTH_WORKER,
    FACILITY_ADMIN,
    DISTRICT_OFFICER,
    GOVERNMENT_ANALYST,
    MOH_ADMIN
}
