package com.motherhood.journey.identity.entity;

/**
 * Role entity placeholder — roles are stored as strings on User.
 * Kept for future role-permission mapping if needed.
 */
public enum Role {
    PATIENT,
    HEALTH_WORKER,
    FACILITY_ADMIN,
    DISTRICT_OFFICER,
    GOVERNMENT_ANALYST,
    MOH_ADMIN
}

