**MotherHood Journey**

Sprint Plan & Task Reference

***IgireRwanda Organization**  ·  **SheCanCode Bootcamp**  ·  **Kigali, Rwanda***

Team: **9** engineers  ·  **5** Backend  ·  **4** Frontend  ·  **5** Sprints  ·  10 Weeks

# **1\. Team Structure**

# **The team consists of nine engineers split across two disciplines. Each engineer owns a clear vertical slice of the system, enabling parallel work with minimal merge conflicts. Backend engineers are assigned by domain module; frontend engineers are assigned by page complexity and component type.**

| ID | Name | Role | Discipline |
| :---- | :---- | :---- | :---- |
| **BE1** | Agnes Mbabazi | Infra & CI/CD lead | **Backend** |
| **BE2** | Ange Umukundwa | Migrations & audit specialist | **Backend** |
| **BE3** | Numukobwa Diane | Core clinical domain | **Backend** |
| **BE4** | Rosine Muhoza | Scheduling & async jobs | **Backend** |
| **BE5** | Clesence Niyirema Sabato | Gov integration & facility stats | **Backend** |
| **FE1** | IMFURANKUNDA Cherly | Data-heavy pages & maps | **Frontend** |
| **FE2** | BYUKUSENGE Immaculee | API client & query infra | **Frontend** |
| **FE3** | UWAYESU Lydie | Forms & validation | **Frontend**  |
| **FE4** | Emelyne | Patient-facing mobile views | **Frontend**  |

# **2\. Sprint Summary**

Five two-week sprints deliver a fully functional system. Each sprint builds on the previous one; no sprint assumes features that have not yet been built.

| Sprint | Focus | Dates | BE Pts | FE Pts | Total |
| :---- | :---- | :---- | ----- | ----- | ----- |
| **Sprint 1** | Foundation & Authentication | Week 1–2 | **32** | **28** | **60** |
| **Sprint 2** | Mother Registration & Pregnancy Management | Week 3–4 | **28** | **28** | **56** |
| **Sprint 3** | Children, Vaccinations & Clinical Vi  sits | Week 5–6 | **31** | **28** | **59** |
| **Sprint 4** | Appointments, Service Requests & Facility Admin | Week 7–8 | **31** | **37** | **68** |
| **Sprint 5** | Government Portal, Reporting & Production Hardening | Week 9–10 | **31** | **39** | **70** |
| **Total** | 5 sprints · 10 weeks |  | **153** | **160** | **313** |

# **3\. Sprint Details Tasks, Descriptions & Folders**

| Sprint 1 Foundation & Authentication | Duration Week 1–2 Story Points  60 pts |
| :---- | :---- |

|  | Sprint goal *Every team member can run the project locally. The database is seeded, Spring Boot serves a protected API, and the Next.js app renders a login page that issues JWTs with roles embedded. No feature works yet  but the scaffold for every feature does.* |
| :---- | :---- |

### **Backend Tasks (BE1–BE5) Sprint 1**

| Who | Backend  Task | Description & folder | Pts |
| :---- | :---- | :---- | ----- |
| **BE1** | **Initialise Maven multi-module project** **Folder:** motherhood-journey/ → pom.xml \+ all submodule pom.xml files | Create the parent POM and all submodule declarations: shared-kernel, domain-geo, domain-identity, domain-facility, domain-maternal, domain-child, domain-appointment, domain-consent, domain-notification, domain-government, infrastructure, and app. Configure the spring-boot-dependencies BOM and shared plugin versions so every module inherits them without repetition. | **3** |
| **BE2** | **Docker Compose for local dev** **Folder:** motherhood-journey/ → docker-compose.yml, .env.example | Write docker-compose.yml with PostgreSQL 15, Redis (for cache), and the Spring Boot app container. Configure health checks so the app waits for the DB to be ready. Add a .env.example file documenting all required environment variables. Every developer runs docker compose to get a fully seeded, working local environment. | **3** |
| **BE2** | **Flyway V1–V3 migrations** **Folder:** app/src/main/resources/db/migration/ → V1\_\_init\_geo\_locations.sql, V2\_\_create\_users.sql, V3\_\_create\_facilities.sql | V1 seeds the complete Rwanda geo hierarchy (5 provinces, 30 districts, 416 sectors, 2148 cells, \~14000 villages) into geo\_locations. V2 creates the users and government\_users tables with all six roles and the scoped\_geo\_ids UUID array. V3 creates the facilities table with the MoH facility code unique index. All indexes defined in the schema document are included. | **5** |
| **BE3** | **User entity, JWT provider and auth filter** **Folder:** domain-identity/src/main/java/.../identity/ → User.java, UserRepository.java, JwtTokenProvider.java, JwtAuthFilter.java | Implement the User JPA entity with the six-role enum, UserRepository with findByNationalId and findByPhoneNumber, and JwtTokenProvider which generates and validates tokens encoding role, facilityId, and geoScopeIds as JWT claims. JwtAuthFilter extends OncePerRequestFilter, reads the bearer token, validates it, and sets the Spring SecurityContext. Failures are logged to audit\_log. | **5** |
| **BE3** | **POST /auth/login and POST /auth/refresh** **Folder:** domain-identity/src/main/java/.../identity/ → AuthService.java, AuthController.java, dto/LoginRequest.java, dto/TokenResponse.java | AuthService.login() validates phone/password, issues an access token (15 min TTL) and refresh token (7 day TTL), and records last\_login on the users row. AuthService.refresh() validates the refresh token and issues a new access token. Both endpoints are public (no JWT required). TokenResponse DTO returns both tokens plus the user's role for the frontend to store in the session. | **3** |
| **BE4** | **GeoLocation entity, service and REST endpoints** **Folder:** domain-geo/src/main/java/.../geo/ → entity/GeoLocation.java, repository/GeoLocationRepository.java, service/, controller/GeoLocationController.java, dto/, mapper/GeoLocationMapper.java | GeoLocation JPA entity with all five hierarchy levels and composite index on (province, district, sector). GeoLocationRepository with 8 finder methods for drill-down queries and NIDA path resolution. GeoLocationService with read-only transactional methods. GeoLocationController exposes GET /api/v1/geo/provinces, /districts, /sectors, /cells, /villages as cascading dropdown endpoints — all public, no JWT required. | **5** |
| **BE5** | **SecurityConfig: JWT filter chain and per-role request matchers** **Folder:** app/src/main/java/.../config/ → SecurityConfig.java, CorsConfig.java | Facility JPA entity, FacilityRepository, FacilityService. The @FacilityScope annotation (in shared-kernel/rbac/) is a composed @PreAuthorize that injects the requesting user's facilityId from the JWT and verifies it matches the resource's facility\_id. Applied to all service methods that query patient data. FacilityController exposes GET /api/v1/facilities with district and type filters for the frontend facility picker.. | **3** |
| **BE1** | **GlobalExceptionHandler and AuditLog skeleton** **Folder:** shared-kernel/src/main/java/.../sharedkernel/ → exception/GlobalExceptionHandler.java, audit/AuditLog.java, audit/AuditService.java, dto/ErrorResponseDTO.java | GlobalExceptionHandler is a @RestControllerAdvice that catches ResourceNotFoundException (→ 404), UnauthorizedException (→ 403), MethodArgumentNotValidException (→ 400), and any unhandled Exception (→ 500). All responses use a standardised ErrorResponseDTO with timestamp, traceId, status, and message. AuditLog entity maps to the audit\_log table. AuditService.log() is a no-op stub that will be completed in Sprint 2\. | **3** |
| **BE1** | **GitHub Actions CI pipeline** **Folder:** .github/workflows/ → ci.yml, deploy.yml | ci.yml runs on every pull request: checkout, set up JDK 21, run mvn verify (compiles, runs unit tests, generates MapStruct code), runs Checkstyle (enforces code style rules in checkstyle.xml), and uploads Jacoco coverage report to Codecov. deploy.yml triggers on merge to main: builds the Docker image, pushes to GitHub Container Registry, and deploys to the Railway staging environment. | **2** |

### **Frontend Tasks (FE1–FE4) Sprint 1**

| Who | Frontend Task | Description & folder | Pts |
| :---- | :---- | :---- | ----- |
| **FE1** | **Initialise Next.js 14 app router project** **Folder:** motherhood-journey-web/ → package.json, tsconfig.json, next.config.ts, tailwind.config.ts | Create the Next.js project with TypeScript, Tailwind CSS, shadcn/ui component library, and the App Router. Configure tsconfig.json with path aliases (@/components, @/lib, @/hooks, @/types). Set up ESLint with the Next.js recommended config and Prettier. Install all Sprint 1 dependencies: next-auth, @tanstack/react-query, zustand, react-hook-form, zod, i18next. | **3** |
| **FE1** | **next-auth configuration with role-aware JWT** **Folder:** lib/auth.ts, types/auth.ts, app/api/auth/\[...nextauth\]/route.ts | Configure NextAuth in lib/auth.ts with a CredentialsProvider that calls POST /auth/login on the Spring Boot API. The JWT callback extracts role, facilityId, geoScopeIds, canExport, and canPushHmis from the API response and stores them in the token. The session callback copies these fields to the session object. Extend next-auth's Session and User TypeScript types in types/auth.ts to include all custom fields. | **5** |
| **FE2** | **middleware.ts: role-based route guard** **Folder:** middleware.ts (project root) | The middleware runs on every request matching /dashboard/:path\*, /(patient)/:path\*, /(health-worker)/:path\*, /(facility-admin)/:path\*, /(district-officer)/:path\*, /(government)/:path\*. It reads the session token using getToken(), checks the role, and redirects to /unauthorized if the role does not match the route group. If there is no session at all, it redirects to /login with a callbackUrl parameter. | **5** |
| **FE3** | **Login page UI with i18n** **Folder:** app/login/page.tsx, lib/i18n/index.ts, lib/i18n/rw.json, lib/i18n/en.json, lib/i18n/fr.json | Login page at app/login/page.tsx with phone number and password inputs, form validation (Zod schema: phone must match Rwanda format \+250XXXXXXXXX, password min 8 chars), error state display, and a language switcher toggling between Kinyarwanda, English, and French. Set up i18next with translation files for all three languages. Translation keys follow the pattern login.title, login.phone, login.password, login.submit, login.error. | **3** |
| **FE4** | **Shared layout components** **Folder:** components/layout/ → Sidebar.tsx, TopBar.tsx, MobileNav.tsx, PageHeader.tsx | Sidebar component renders navigation links based on the user's role from useRole(). Each role portal has its own nav link set. TopBar shows the user's name, facility/ministry name, and a logout button. MobileNav renders a bottom tab bar for patient and health worker portals (mobile-first). PageHeader is a reusable section header with title, optional subtitle, and action slot. All components use shadcn/ui primitives. | **5** |
| **FE2** | **lib/api/ base client** **Folder:** lib/api/client.ts, lib/api/geo.ts, types/api.ts | lib/api/client.ts is a typed fetch wrapper that auto-attaches the bearer token from the NextAuth session to every request. It throws a typed ApiError on non-2xx responses (includes status code and error message from the ErrorResponseDTO). lib/api/geo.ts exports getProvinces(), getDistricts(province), getSectors(district), getCells(sector), getVillages(cell) all typed with the GeoLocationDTO shape from types/api.ts. | **3** |
| **FE3** | **lib/rbac.ts and useRole hook** **Folder:** lib/rbac.ts, hooks/useRole.ts | lib/rbac.ts exports hasRole(session, role): boolean and canAccess(session, resource): boolean. useRole() hook returns the current user's role from the NextAuth session. useCanAccess(resource) returns a boolean gate used for conditional rendering of UI elements (e.g. show the HMIS push button only for MOH\_ADMIN). These are client-side helpers; the real enforcement is in middleware.ts. | **2** |
| **FE1** | **Deploy to preview environments** **Folder:** vercel.json, .github/workflows/deploy.yml, CONTRIBUTING.md | Configure Vercel project linked to the GitHub repository. Every PR automatically gets a Vercel preview deployment. Set NEXTAUTH\_URL, NEXTAUTH\_SECRET, and NEXT\_PUBLIC\_API\_BASE\_URL as Vercel environment variables. Configure the Spring Boot app Docker image deployment to Railway staging using the GitHub Actions deploy.yml workflow. Document the deployment process in CONTRIBUTING.md. | **2** |

| Sprint 2 Mother Registration & Pregnancy Management | Duration Week 3–4 Story Points  56 pts |
| :---- | :---- |

|  | Sprint goal *A health worker can register a mother, verify her identity via NIDA, open a pregnancy, assign a CHW, and a patient can log in and see her own pregnancy summary. The patient portal renders real data for the first time.* |
| :---- | :---- |

### **Backend Tasks (BE1–BE5)  Sprint 2**

| Who | Backend Task | Description & folder | Pts |
| :---- | :---- | :---- | ----- |
| **BE2** | **Flyway V4: mothers and pregnancies tables** **Folder:** app/src/main/resources/db/migration/ → V4\_\_create\_mothers\_pregnancies.sql | V4 creates the mothers table (with NIDA verified status, health\_id unique index, education level) and the pregnancies table (with gravida, para, LMP, EDD, assigned\_chw\_id FK, status enum). All indexes defined in the schema document are created: idx\_mother\_health\_id, idx\_mother\_facility, idx\_mother\_geo, idx\_mother\_nida\_status, idx\_pregnancy\_mother, idx\_pregnancy\_chw, idx\_pregnancy\_status. | **2** |
| **BE3** | **Mother entity, repository, service, mapper, DTOs** **Folder:** domain-maternal/src/main/java/.../maternal/ → entity/Mother.java, repository/MotherRepository.java, service/MotherService.java, mapper/MotherMapper.java, dto/ | Mother JPA entity with all schema fields. MotherRepository with finders: findByHealthId, findByUserIdAndFacilityId, findByNidaVerifiedStatus. MotherService with registerMother() which generates a unique health\_id (format: MH-2026-XXXXXX), validates that the mother does not already exist by NID, and triggers async NIDA verification. MotherMapper (MapStruct) converts between entity and MotherDTO/MotherSummaryDTO. | **5** |
| **BE3** | **POST /api/v1/mothers with async NIDA cross-check** **Folder:** domain-maternal/.../maternal/controller/MotherController.java, service/MotherService.java, infrastructure/nida/NidaApiClient.java | The registration endpoint validates the request body (Zod-equivalent: @Valid \+ MotherRegistrationRequest), creates the Mother row with NIDA\_VERIFIED\_STATUS=PENDING, and returns immediately with the generated health\_id. An @Async method then calls NidaApiClient.verifyIdentity() using the provided national ID, and updates the mother's nida\_verified\_status to VERIFIED, FAILED, or MANUAL based on the NIDA response. NidaApiClient is a stub returning VERIFIED in Sprint 1\. | **5** |
| **BE4** | **Pregnancy entity and service** **Folder:** domain-maternal/.../maternal/ → entity/Pregnancy.java, service/PregnancyService.java, controller/PregnancyController.java, dto/PregnancyDTO.java | Pregnancy JPA entity. PregnancyService.openPregnancy() creates a new pregnancy for a mother (validating she has no other ACTIVE pregnancy), computes EDD \= LMP date \+ 280 days, and sets status=ACTIVE. PregnancyService.assignChw() updates the assigned\_chw\_id FK. PregnancyService.closePregnancy() transitions status to DELIVERED, LOST, or TRANSFERRED and records outcome\_notes. GET /api/v1/mothers/{id}/pregnancies returns the full obstetric history. | **5** |
| **BE4** | **GET /api/v1/mothers/{id} and pregnancy endpoints** **Folder:** domain-maternal/.../maternal/controller/MotherController.java, service/MotherService.java | GET /api/v1/mothers/{id} returns the full MotherDTO including geo location summary, facility name, NIDA status, and current pregnancy summary. It enforces facility scope: a HEALTH\_WORKER at Kacyiru cannot retrieve a mother registered at Remera the service checks that the mother's facility\_id matches the requesting user's facility\_id from their JWT. FACILITY\_ADMIN sees all mothers in their facility. DISTRICT\_OFFICER sees mothers in their scoped sectors. | **3** |
| **BE5** | **Facility entity and multi-tenancy enforcement** **Folder:** domain-facility/.../facility/ → entity/Facility.java, repository/FacilityRepository.java, service/FacilityService.java, controller/FacilityController.java; shared-kernel/.../rbac/FacilityScope.java | Facility JPA entity, FacilityRepository, FacilityService. The @FacilityScope annotation (in shared-kernel/rbac/) is a composed @PreAuthorize that injects the requesting user's facilityId from the JWT and verifies it matches the resource's facility\_id. Applied to all service methods that query patient data. FacilityController exposes GET /api/v1/facilities with district and type filters for the frontend facility picker. | **3** |
| **BE2** | **Consent module** **Folder:** domain-consent/.../consent/ → entity/ConsentRecord.java, service/ConsentService.java, controller/ConsentController.java | ConsentRecord JPA entity with consent\_type enum (GOV\_DATA\_SHARE, SMS\_REMINDERS, RESEARCH, FACILITY\_TRANSFER), granted boolean, legal\_basis, expires\_at, and revoked\_at. ConsentService.hasActiveConsent(motherId, type) is the gate called by any service that shares data with government systems; it checks that a non-revoked, non-expired GOV\_DATA\_SHARE consent exists before allowing the data to flow. POST /api/v1/consent records a new consent. DELETE /api/v1/consent/{id} records revocation. | **3** |
| **BE1** | **AuditAspect implementation** **Folder:** shared-kernel/.../audit/ → AuditAspect.java, AuditService.java, AuditLog.java; shared-kernel/.../audit/AuditedResource.java (annotation) | AuditAspect is a Spring @Aspect that intercepts all methods annotated with @AuditedResource. It extracts the authenticated user from the SecurityContext, the resource type and ID from the method arguments, the client IP from the HTTP request, and the user agent. It calls AuditService.log() asynchronously (@Async) to write the audit\_log row without blocking the main request thread. Applied to all MotherController and PregnancyController methods in this sprint. | **2** |

### **Frontend Tasks (FE1–FE4) Sprint 2**

| Who | Frontend Task | Description & folder | Pts |
| :---- | :---- | :---- | ----- |
| **FE2** | **GeoLocationSelect cascading dropdown component** **Folder:** components/forms/GeoLocationSelect.tsx, hooks/useGeoLocation.ts, lib/api/geo.ts | GeoLocationSelect is a React component that renders five cascading select inputs: Province → District → Sector → Cell → Village. Each level fetches its options from the geo API only when the parent level has a value selected (using react-query's enabled option). On province change, district/sector/cell/village are reset. The component accepts an onChange callback with the full selected GeoLocationSummaryDTO. Used in the mother registration form and anywhere geo selection is needed. | **cc** |
| **FE3** | **Mother registration form** **Folder:** app/(health-worker)/mothers/register/page.tsx, lib/schemas/motherSchema.ts, lib/api/mothers.ts | Multi-step form using react-hook-form with Zod validation. Step 1: National ID input with Rwanda NID format validation (16-digit number), first name, last name, date of birth, phone number. Step 2: GeoLocationSelect for home location, education level dropdown. Step 3: Confirmation summary before submit. On submit, calls POST /api/v1/mothers and displays the generated health\_id in a success state with a print button (for facilities without printers, the health\_id is the shareable identifier). | **5** |
| **FE4** | **Mother search and list page** **Folder:** app/(health-worker)/mothers/page.tsx, hooks/useMother.ts, lib/api/mothers.ts | c | **3** |
| **FE1** | **Mother profile page** **Folder:** app/(health-worker)/mothers/\[motherId\]/page.tsx, lib/api/mothers.ts, components/status/NidaStatusBadge.tsx | Shows the mother's personal details, geo location (village/cell/sector), NIDA verification status badge, and the assigned CHW's name and phone number. Below the header is a pregnancy timeline: each pregnancy is a card showing gravida/para numbers, LMP date, EDD, status badge, and a link to the visit history. The current active pregnancy is highlighted. An action button lets the health worker open a new pregnancy or close the current one. | **5** |
| **FE2** | **Patient dashboard** **Folder:** app/(patient)/dashboard/page.tsx, hooks/useMother.ts, hooks/useVaccinations.ts | The patient's home screen. Shows an active pregnancy card (EDD countdown in days, trimester indicator, assigned CHW contact card). Below it, a children list summary showing each child's name, age, and vaccination status (a green/amber/red pill). At the bottom, the next upcoming appointment with date, time, facility name, and appointment type. All data fetched in parallel using react-query. Skeleton loading states for each section. | **5** |
| **FE3** | **Patient consent management page** **Folder:** app/(patient)/consent/page.tsx, lib/api/consent.ts | Shows four consent types as toggle cards: Government Data Sharing, SMS Reminders, Research Participation, Facility Transfer. Each card shows the consent type description, current status (GRANTED/REVOKED), the date it was granted, the legal basis (Rwanda Law No. 058/2021), and an expiry date if applicable. Toggling calls POST /api/v1/consent or DELETE /api/v1/consent/{id}. A warning dialog confirms before revoking GOV\_DATA\_SHARE consent. | **3** |
| **FE4** | **Shared status components** **Folder:** components/status/NidaStatusBadge.tsx, components/status/VaccinationStatusPill.tsx | NidaStatusBadge renders a colour-coded pill for PENDING (amber), VERIFIED (green), FAILED (red), MANUAL (gray). VaccinationStatusPill renders PENDING (gray), ADMINISTERED (green), MISSED (amber), OVERDUE (red). Both accept a status string prop and render consistently across all portals. Used on the mother list, mother profile, child profile, and vaccination schedule pages. | **2** |

| Sprint 3 Children, Vaccinations & Clinical Visits | Duration Week 5–6 Story Points  59 pts |
| :---- | :---- |

|  | Sprint goal *Health workers can register newborns (auto-creating the full EPI schedule), record clinical visits with vitals and diagnoses, and mark vaccine doses as administered. Patients can see their children's vaccination cards. Overdue vaccinations trigger SMS via Africa's Talking.* |
| :---- | :---- |

### **Backend Tasks (BE1–BE5) Sprint 3**

| Who | Backend Task | Description & folder | Pts |
| :---- | :---- | :---- | ----- |
| **BE2** | **Flyway V5–V6 migrations** **Folder:** app/src/main/resources/db/migration/ → V5\_\_create\_children\_vaccination.sql, V6\_\_create\_visits\_diagnoses.sql | V5 creates the children table (birth\_certificate\_no unique index, health\_status index), vaccination\_schedules table (seeded with the full Rwanda EPI schedule: BCG, OPV0, OPV1-3, PENTA1-3, ROTA1-2, PCV1-3, MR, MMR, and others), and vaccination\_records table with the composite unique index (child\_id, schedule\_id). V6 creates health\_visits (with the polymorphic patient\_ref\_id \+ patient\_type columns), diagnoses, and prescriptions tables. | **3** |
| **BE3** | **Child entity and ChildService** **Folder:** domain-child/.../child/ → entity/Child.java, entity/VaccinationRecord.java, entity/VaccinationSchedule.java, service/ChildService.java, controller/ChildController.java, repository/, dto/, mapper/ | Child JPA entity. ChildService.registerChild() creates the child row linked to a mother, records birth weight and delivery type, and then automatically creates one VaccinationRecord row per entry in vaccination\_schedules computing the due\_date as child.dateOfBirth plus the schedule's due\_age\_days. This means every child born in the system immediately has a complete, personalised vaccination schedule ready to track without any manual entry. | **5** |
| **BE4** | **VaccinationService and overdue scanner** **Folder:** domain-child/.../child/ → service/VaccinationService.java; app/.../config/SchedulerConfig.java | VaccinationService.markAdministered() updates a VaccinationRecord's status to ADMINISTERED, records the administered\_date and lot\_number, and sets the administered\_by FK to the health worker's user ID. A @Scheduled cron job runs nightly at 01:00 Rwanda time (Africa/Kigali timezone): it scans all VaccinationRecord rows with status=PENDING where due\_date \+ window\_days \< today and flips them to OVERDUE. Each OVERDUE flip calls SmsService.enqueue() to schedule a reminder SMS. | **5** |
| **BE5** | **HealthVisit polymorphic entity and service** **Folder:** domain-maternal/.../maternal/ → entity/HealthVisit.java, entity/Diagnosis.java, entity/Prescription.java, service/HealthVisitService.java, controller/HealthVisitController.java, dto/ | HealthVisit JPA entity uses a patient\_type VARCHAR column (MOTHER or CHILD) and a patient\_ref\_id UUID that points to either mothers.id or children.id — a polymorphic reference without a strict FK constraint. HealthVisitService.recordVisit() accepts a VisitRequest containing patient type and ID, visit type (ANC, PNC, IMMUNIZATION, SICK\_CHILD, GROWTH\_MONITORING), vitals (weight, height, BP, MUAC), and a list of DiagnosisRequests. It creates the visit, diagnosis, and prescription rows in a single transaction. | **5** |
| **BE3** | **Diagnosis and Prescription entities** **Folder:** domain-maternal/.../maternal/ → entity/Diagnosis.java, entity/Prescription.java, dto/DiagnosisDTO.java, dto/PrescriptionDTO.java | Diagnostic entity with ICD-10 code (validated against a seeded subset from MoH HMIS), severity (MILD, MODERATE, SEVERE), and is\_primary flag. A visit can have multiple diagnoses; exactly one should be flagged is\_primary for analytics aggregation. Prescription entity linked to a visit with medication\_name, dosage, frequency, and duration\_days. The duration\_days field will be used in a later sprint to schedule medication adherence SMS reminders. | **3** |
| **BE1** | **SmsNotification entity, SmsService and Africa's Talking client** **Folder:** domain-notification/.../notification/ → entity/SmsNotification.java, service/SmsService.java, AfricasTalkingClient.java, controller/SmsNotificationController.java (webhook) | SmsNotification entity maps to sms\_notifications with QUEUED → SENT → DELIVERED | FAILED lifecycle. SmsService.enqueue() creates a QUEUED row with a scheduled\_at timestamp. A @Scheduled cron runs every 5 minutes, scans QUEUED rows where scheduled\_at \<= now(), and calls AfricasTalkingClient.sendSms() for each. AfricasTalkingClient POSTs to the AT REST API, stores the returned at\_message\_id, and updates the row to SENT. On failure, it increments retry\_count (max 3\) and reschedules. | **5** |
| **BE2** | **Africa's Talking delivery status webhook** **Folder:** domain-notification/.../notification/controller/SmsNotificationController.java | POST /webhooks/at/delivery receives delivery status callbacks from Africa's Talking. The endpoint is public (no JWT) but validates an HMAC-SHA256 signature using the AT API key to prevent spoofing. It looks up the SmsNotification by at\_message\_id and updates the status to DELIVERED or FAILED. This closes the loop on the SMS outbox pattern — the system knows definitively whether each message was delivered. | **2** |
| **BE4 \+ BE5+BE3** | **Unit tests: ChildService, VaccinationService, SmsService** **Folder:** domain-child/src/test/ → service/ChildServiceTest.java, service/VaccinationServiceTest.java; domain-notification/src/test/ → service/SmsServiceTest.java | Pure Mockito unit tests — no Spring context, no database. ChildService tests: verify that registering a child creates exactly N vaccination records (one per active EPI schedule entry), verify health\_id generation, verify that registering a child for an unknown mother throws MotherNotFoundException. VaccinationService tests: markAdministered sets correct fields, overdue scanner flips exactly the right records, SMS is enqueued per OVERDUE flip. SmsService tests: retry count increments, max retries respected. | **3** |

### **Frontend Tasks (FE1–FE4) Sprint 3**

| Who | Frontend Task | Description & folder | Pts |
| :---- | :---- | :---- | ----- |
| **FE1** | **Newborn registration form** **Folder:** app/(health-worker)/children/register/page.tsx, lib/schemas/childSchema.ts, lib/api/children.ts | Form for health workers to register a newborn. Links to the mother's record via health\_id search (autocomplete). Fields: child first name, gender, date of birth, birth weight (kg), delivery type (NORMAL, CAESAREAN, ASSISTED), birth certificate number. On submit, calls POST /api/v1/children and displays a success screen showing the child's generated record and a summary of the vaccination schedule that was auto-created (listing each vaccine name and due date). | **5** |
| **FE2** | **Child profile and vaccination schedule table** **Folder:** app/(health-worker)/mothers/\[motherId\]/page.tsx (children section), app/(health-worker)/children/\[childId\]/page.tsx, hooks/useVaccinations.ts | The child profile page shows a health status badge (HEALTHY/AT\_RISK/CRITICAL), birth details, delivery type, birth weight, and mother's name link. Below is the vaccination schedule table: columns are Vaccine, Antigen Code, Due Date, Window, Status, Administered Date, Lot Number. Rows are colour-coded by status (gray=PENDING, green=ADMINISTERED, amber=MISSED, red=OVERDUE). Clicking an OVERDUE or PENDING row opens a mark-administered dialog. | **5** |
| **FE3** | **Patient vaccination card (mobile-first)** **Folder:** app/(patient)/children/\[childId\]/vaccinations/page.tsx | The patient-facing view of their child's vaccination card. Designed for mobile: large text, high contrast, printable layout. Each vaccine is a card with the vaccine name, status icon, due date, and administered date. A progress indicator at the top shows 'X of Y vaccines completed'. Overdue vaccines are highlighted with a red banner and a note to contact the CHW. The page is fully functional offline (service worker caches the last-fetched data for 24 hours). | **5** |
| **FE4** | **Vaccination session page for health workers** **Folder:** app/(health-worker)/vaccinations/page.tsx, lib/api/children.ts | Health workers use this page during a vaccination session. A search input accepts the child's health\_id or birth certificate number. On match, the child's name, age, and today's due vaccines are shown. Each due vaccine has a 'Mark administered' button that opens a dialog to enter the lot number. On confirm, calls PUT /api/v1/vaccinations/{id}/administer, invalidates the vaccination query cache, and shows a success toast. The page stays open for the next child. | **5** |
| **FE1** | **Visit recording form** **Folder:** app/(health-worker)/visits/new/page.tsx, lib/schemas/visitSchema.ts, lib/api/visits.ts | Form for health workers to record a new clinical visit. Step 1: Select patient type (Mother or Child) and search by health\_id. Step 2: Select visit type (ANC, PNC, IMMUNIZATION, SICK\_CHILD, GROWTH\_MONITORING) and record vitals — weight (kg), height (cm), systolic and diastolic BP, MUAC (cm). Step 3: Add diagnoses using a searchable ICD-10 dropdown (subset loaded from the API), mark one as primary, add free-text notes. Step 4: Optionally add prescriptions with medication name, dosage, frequency, and duration. | **5** |
| **FE2** | **useVaccinations and useChildren hooks** **Folder:** hooks/useVaccinations.ts, hooks/useChildren.ts, lib/query-keys.ts | useVaccinationSchedule(childId) fetches GET /api/v1/children/{id}/vaccinations and returns the typed VaccinationRecordDTO array with isLoading and error states. useMarkAdministered() returns a mutation function that calls PUT /api/v1/vaccinations/{id}/administer and on success invalidates the useVaccinationSchedule query — triggering an automatic refetch across all components displaying that child's schedule. lib/query-keys.ts centralises all query keys to prevent cache key collisions. | **3** |

| Sprint 4 Appointments, Service Requests & Facility Admin | Duration Week 7–8 Story Points  68 pts |
| :---- | :---- |

|  | Sprint goal *The appointment scheduler is live with SMS reminders. Citizens can submit government service requests (birth certificate, vaccination card) that facility administers review and route to Irembo. The facility admin dashboard shows real KPIs: ANC attendance, vaccination coverage, no-show rate.* |
| :---- | :---- |

### **Backend Tasks (BE1–BE5) Sprint 4**

| Who | Backend Task | Description & folder | Pts |
| :---- | :---- | :---- | ----- |
| **BE3** | **Appointment entity and AppointmentService** **Folder:** domain-appointment/.../appointment/ → entity/Appointment.java, service/AppointmentService.java, controller/AppointmentController.java, dto/, repository/AppointmentRepository.java | Appointment JPA entity with polymorphic patient reference (same pattern as HealthVisit), appointment\_type enum, status lifecycle (SCHEDULED → COMPLETED | NO\_SHOW | CANCELLED), and reminder\_sent boolean flag. AppointmentService.schedule() validates that the facility has capacity on the requested datetime and creates the appointment. AppointmentService.cancel() transitions status and records a cancellation reason. AppointmentService.updateStatus() is called by health workers to mark COMPLETED or NO\_SHOW after the appointment time. | **5** |
| **BE4** | **Appointment reminder cron** **Folder:** domain-appointment/.../appointment/service/AppointmentService.java, app/.../config/SchedulerConfig.java | A @Scheduled cron runs at 08:00 Rwanda time daily. It queries appointments where scheduled\_at is between now+23h and now+25h and reminder\_sent=false. For each result, it calls SmsService.enqueue() with a message in the patient's preferred\_language (Kinyarwanda, English, or French) containing the appointment date, time, facility name, and appointment type. After enqueuing, it sets reminder\_sent=true on the appointment row to prevent duplicate reminders. | **3** |
| **BE2** | **Flyway V9: government integration tables** **Folder:** app/src/main/resources/db/migration/ → V9\_\_create\_gov\_integration.sql | Creates service\_requests (with the reference\_no unique index SR-2026-XXXXX format generator, irembo\_ticket\_id, payload JSONB, rejection\_reason), service\_request\_docs (with file\_path and SHA-256 file\_hash), gov\_sync\_log (with idempotency\_key unique index, payload\_hash, retry\_count, next\_retry\_at, dead\_letter detection), and gov\_reports (with aggregates JSONB and hmis\_push\_status). | **2** |
| **BE5** | **ServiceRequestService and routing** **Folder:** domain-government/.../government/ → entity/ServiceRequest.java, service/ServiceRequestService.java, controller/ServiceRequestController.java | ServiceRequestService.submit() generates a unique reference\_no (SR-YYYY-NNNNN), creates the ServiceRequest row with status=PENDING, validates that the mother has an active GOV\_DATA\_SHARE consent before accepting the request, and creates a GovSyncLog entry for the outbox. The FACILITY\_ADMIN review actions (approve, reject) are exposed via PUT /api/v1/service-requests/{id}/review. Rejection records a rejection\_reason. Approval triggers escalation to DISTRICT\_OFFICER or direct Irembo submission depending on service\_type. | **5** |
| **BE1** | **IremboApiClient and GovSyncService outbox processor** **Folder:** domain-government/.../government/ → entity/GovSyncLog.java, service/GovSyncService.java, IremboApiClient.java, NidaApiClient.java (upgraded from stub) | GovSyncService is a @Scheduled job running every 2 minutes. It queries GovSyncLog rows with status=PENDING and next\_retry\_at \<= now(). For each, it sets status=IN\_FLIGHT, calls the appropriate external API (NidaApiClient, HmisApiClient, or IremboApiClient) based on target\_system, and sets status=SUCCEEDED or FAILED. On failure, it increments retry\_count and sets next\_retry\_at using exponential backoff (2^retry\_count minutes). After 5 failures, status becomes DEAD\_LETTER and an admin SMS alert is sent. | **5** |
| **BE2** | **ServiceRequestDoc upload and SHA-256 verification** **Folder:** domain-government/.../government/ → entity/ServiceRequestDoc.java, service/ServiceRequestService.java; infrastructure/storage/StorageClient.java | ServiceRequestDoc entity maps to service\_request\_docs. POST /api/v1/service-requests/{id}/docs accepts a multipart file upload. The service reads the file bytes, computes a SHA-256 hash, stores the file in the configured storage bucket (S3-compatible), and saves the ServiceRequestDoc row with the file\_path and file\_hash. On retrieval, the hash is recomputed and compared to detect any tampering. The storage adapter in infrastructure/storage/ abstracts the bucket implementation. | **3** |
| **BE3** | **GET /api/v1/facilities/{id}/stats** **Folder:** domain-facility/.../facility/ → service/FacilityStatsService.java, controller/FacilityController.java, dto/FacilityStatsDTO.java | Aggregation endpoint used by the facility admin dashboard. Returns: anc\_attendance\_rate (ANC visits this month / expected ANC visits based on active pregnancies), vaccination\_coverage\_pct (ADMINISTERED records / total records for this facility's children), no\_show\_rate (NO\_SHOW appointments / total SCHEDULED appointments in the last 30 days), and service\_request\_backlog (count of PENDING \+ UNDER\_REVIEW requests). All computed with JPQL aggregate queries scoped to the facility\_id. | **5** |
| **BE4** | **Integration tests: Appointment and ServiceRequest repositories** **Folder:** domain-appointment/src/test/java/.../appointment/repository/AppointmentRepositoryTest.java, domain-government/src/test/java/.../government/repository/ServiceRequestRepositoryTest.java | @DataJpaTest integration tests using a Testcontainers PostgreSQL instance (migrations run automatically via Flyway). AppointmentRepositoryTest: verify findByFacilityIdAndScheduledAtBetweenAndReminderSentFalse returns exactly the right rows, verify status transition persists correctly. ServiceRequestRepositoryTest: verify reference\_no uniqueness constraint, verify payload JSONB stores and retrieves correctly, verify status index is used by EXPLAIN ANALYZE. | **3** |

### **Frontend Tasks (FE1–FE4) Sprint 4**

| Who | Frontend  Task | Description & folder | Pts |
| :---- | :---- | :---- | ----- |
| **FE3** | **Appointment scheduling form for health workers** **Folder:** app/(health-worker)/appointments/new/page.tsx, lib/api/appointments.ts, lib/schemas/appointmentSchema.ts | Form to schedule an appointment for a mother or child. Patient search by health\_id at the top. Appointment type selector (ANC, PNC, VACCINATION, GROWTH\_CHECK, FOLLOW\_UP). Date and time picker with available slots shown (greyed-out times that are fully booked). Facility auto-filled from the health worker's JWT. Optional note field. On submit, call POST /api/v1/appointments. On success, shows confirmation with reference number and a 'Send SMS reminder' toggle. | **5** |
| **FE4** | **Patient appointments page** **Folder:** app/(patient)/appointments/page.tsx, hooks/useAppointments.ts | Lists all the patient's appointments in chronological order. Future appointments show date, time, facility name, appointment type, and status badge (SCHEDULED in blue). Past appointments show COMPLETED (green), NO\_SHOW (amber), or CANCELLED (gray). Tapping a future appointment shows a detailed view with a 'Cancel appointment' button (which calls DELETE /api/v1/appointments/{id} and shows a confirmation dialog). A floating action button links to the request form for new service requests. | **3** |
| **FE1** | **Service request submission form (patient)** **Folder:** app/(patient)/documents/page.tsx, lib/api/serviceRequests.ts | Multi-step form for submitting a government service request. Step 1: Service type selection (BIRTH\_CERT, VACCINATION\_CARD, REFERRAL, HEALTH\_SUMMARY, REPRINT) with descriptions of what each requires. Step 2: Required documents upload (ID\_COPY, BIRTH\_PROOF, FACILITY\_LETTER) with drag-and-drop and file type/size validation. Step 3: Review summary before submit. On success: display the reference number (SR-2026-XXXXX) prominently with instructions to track status. | **5** |
| **FE2** | **ServiceRequestTracker component** **Folder:** components/status/ServiceRequestTracker.tsx | A horizontal stepper component showing the service request lifecycle: PENDING → UNDER\_REVIEW → APPROVED → IREMBO\_SUBMITTED → COMPLETED. Each step shows the status label, the date it was reached, and for REJECTED status, the rejection reason is shown in a red callout below the tracker. The component accepts a ServiceRequestDTO prop and derives the current step from the status field. Used on the patient documents page and the facility admin review queue. | **3** |
| **FE1** | **Facility admin service request review queue** **Folder:** app/(facility-admin)/service-requests/page.tsx, lib/api/serviceRequests.ts | Table view of all service requests for the facility, filterable by status and service\_type. Each row shows: reference\_no, patient name, service type, submitted date, status badge, and an Actions column. The Actions column shows 'Review' for PENDING requests (opens a side panel with the full request details and uploaded document previews), 'Approve' (PUT .../review with status=APPROVED), and 'Reject' (opens a dialog to enter rejection\_reason). Escalation to DISTRICT\_OFFICER is a third action for eligible requests. | **5** |
| **FE3** | **Facility admin KPI dashboard** **Folder:** app/(facility-admin)/dashboard/page.tsx, components/charts/AncAttendanceTrend.tsx, components/charts/VaccinationCoverageChart.tsx, components/charts/NoShowRateBar.tsx | The centrepiece of the facility admin portal. Four stat cards at the top: ANC attendance rate, vaccination coverage %, no-show rate, service request backlog — all from GET /api/v1/facilities/{id}/stats. Below: a Recharts LineChart showing ANC attendance over the last 12 months, a BarChart showing vaccination coverage by vaccine type, and a small table of the 5 most recent service requests with status badges. Date range picker filters all charts. Data refetches every 5 minutes using react-query's refetchInterval. | **8** |
| **FE4** | **Facility admin staff management** **Folder:** app/(facility-admin)/staff/page.tsx, lib/api/users.ts | List of all HEALTH\_WORKER accounts at the facility. Table columns: name, phone number, national ID, account status (active/inactive badge), last login date. Actions: activate (PATCH .../users/{id}/activate) and deactivate (with a confirmation dialog warning that the health worker will lose access). A 'New staff account' button links to the identity module's user creation flow. Deactivated staff appear greyed out at the bottom of the list. | **3** |
| **FE2** | **Facility admin appointment calendar** **Folder:** app/(facility-admin)/appointments/page.tsx, components/charts/ (calendar grid) | A week-view calendar grid for the facility. The X-axis is days of the week, the Y-axis is hours (07:00–18:00 Rwanda time). Each appointment is a coloured block in its time slot, colour-coded by appointment type. Hovering shows the patient name and appointment type. Clicking opens the appointment detail side panel. A capacity bar below the calendar shows how many slots are booked vs available for each day. Navigation arrows move between weeks. | **5** |

| Sprint 5 Government Portal, Reporting & Production Hardening | Duration Week 9–10 Story Points  70 pts |
| :---- | :---- |

|  | Sprint goal *District officers view sector-scoped data. Government analysts generate reports and push them to MoH HMIS. MOH admins manage government user accounts and monitor the sync log. The app passes end-to-end tests, WCAG accessibility audit, and is deployed to production with monitoring.* |
| :---- | :---- |

### **Backend Tasks (BE1–BE5) Sprint 5**

| Who | Backend Task | Description & folder | Pts |
| :---- | :---- | :---- | ----- |
| **BE5** | **GovReportService: aggregate anonymised stats** **Folder:** domain-government/.../government/ → entity/GovReport.java, service/GovReportService.java, controller/GovReportController.java, dto/GovReportRequest.java, dto/GovReportDTO.java | GovReportService.generateReport() accepts a report\_type (VACCINATION\_COVERAGE, ANC\_ATTENDANCE, BIRTH\_REGISTRATION, MATERNAL\_HEALTH), period string (e.g. 2026-Q1), and scope\_level (NATIONAL, PROVINCE, DISTRICT, SECTOR). It runs JPQL aggregate queries grouped by the chosen scope level, builds the aggregates JSONB object (vaccination\_coverage\_pct, anc\_visits\_total, birth\_registrations\_total, maternal\_mortality\_proxy, etc.), and saves a GovReport row. The aggregates contain zero individual patient data — only computed statistics. | **5** |
| **BE1** | **HmisApiClient: push reports to MoH HMIS** **Folder:** domain-government/.../government/ → HmisApiClient.java, service/GovReportService.java (pushToHmis method) | HmisApiClient formats the GovReport aggregates JSONB into the MoH HMIS API payload schema (DHIS2-compatible format). It creates a GovSyncLog entry with target\_system=HMIS and sync\_type=REPORT\_PUSH, then the existing GovSyncService outbox processor picks it up and executes the API call. GovReport.hmis\_push\_status is updated to QUEUED, PUSHED, or FAILED based on the sync result. This keeps all external API calls in the outbox pattern — no blocking HTTP calls from user-facing requests. | **5** |
| **BE3** | **RbacUtils.canAccessGeo(): sector-scope enforcement** **Folder:** shared-kernel/.../rbac/ → RbacUtils.java, FacilityScope.java; domain-government/.../government/controller/GovReportController.java (@PreAuthorize annotations) | RbacUtils is a Spring @Component whose methods are called from @PreAuthorize SPeL expressions. canAccessGeo(authentication, geoLocationId) extracts the user's role and geoScopeIds from the JWT. For DISTRICT\_OFFICER, it calls GeoLocationRepository.findById(geoLocationId) to get the sector, then checks whether that sector's UUID is in the user's scoped\_geo\_ids array. For GOVERNMENT\_ANALYST and MOH\_ADMIN, it returns true (national scope). For HEALTH\_WORKER and FACILITY\_ADMIN, it delegates to facility-scope checks. | **5** |
| **BE2** | **government\_users CRUD for MOH\_ADMIN** **Folder:** domain-government/.../government/ → entity/GovernmentUser.java, service/GovernmentUserService.java, controller/GovernmentUserController.java | GovernmentUser entity with scoped\_geo\_ids UUID\[\] array (PostgreSQL array type). GovernmentUserService.assignGeoScope() replaces the scoped\_geo\_ids array for a DISTRICT\_OFFICER. GovernmentUserService.togglePermission() sets can\_export or can\_push\_hmis. GovernmentUserController exposes these as PATCH /api/v1/government-users/{id}/geo-scope and PATCH /api/v1/government-users/{id}/permissions — both restricted to MOH\_ADMIN via @PreAuthorize. Includes list and get-by-id endpoints. | **3** |
| **BE4** | **DEAD\_LETTER alert and gov\_sync\_log monitoring** **Folder:** domain-government/.../government/ → service/GovSyncService.java, controller/GovSyncController.java | When GovSyncService sets a GovSyncLog row to DEAD\_LETTER (after 5 failed retries), it calls SmsService.enqueue() to send an alert SMS to all users with MOH\_ADMIN role. The message includes the sync\_type, target\_system, and reference\_no of the failed request. GET /api/v1/gov-sync-log exposes the log for the government portal's sync log page, filterable by status and target\_system. Manual retry is exposed as POST /api/v1/gov-sync-log/{id}/retry (MOH\_ADMIN only). | **3** |
| **BE1 \+ BE2** | **End-to-end API tests with @SpringBootTest** **Folder:** app/src/test/java/.../e2e/ → MotherRegistrationE2ETest.java, ServiceRequestE2ETest.java, GovReportE2ETest.java | Full stack integration tests that load the entire Spring context against a Testcontainers PostgreSQL database. Test flows: (1) Register a mother → open pregnancy → register child → vaccination schedule auto-created → mark vaccination administered → verify audit\_log has READ and UPDATE entries. (2) Submit service request → facility admin approves → gov\_sync\_log entry created → GovSyncService processes it → IREMBO\_SUBMITTED status confirmed. (3) Generate a GovReport → push to HMIS → verify hmis\_push\_status=PUSHED. | **5** |
| **BE5** | **Production hardening** **Folder:** app/src/main/java/.../config/ → ProductionConfig.java; app/src/main/resources/ → application-prod.yml; DEPLOYMENT.md | Configure HikariCP connection pool with pool size tuned for Railway's PostgreSQL plan (max 10 connections). Configure audit\_log table with monthly partitioning using Flyway V10 (CREATE TABLE audit\_log\_2026\_04 PARTITION OF audit\_log). Add Spring Boot Actuator health endpoints (/actuator/health, /actuator/info) with custom health indicators for the Africa's Talking API and the NIDA API. Configure Flyway baseline for the production database migration. Document all environment variables required in production in a DEPLOYMENT.md file. | **3** |
| **BE3** | **OpenAPI docs review and README** **Folder:** app/src/main/java/.../config/OpenApiConfig.java; README.md, ARCHITECTURE.md | Review the auto-generated Springdoc OpenAPI 3.1 specification at /v3/api-docs. Ensure all endpoints have @Operation summaries, @Parameter descriptions, and @ApiResponse annotations for 200, 400, 401, 403, and 404 responses. Verify the JWT bearer security scheme is correctly applied to all protected endpoints. Write the project README.md with: prerequisites, local setup with Docker Compose, running tests, API documentation link, and architecture overview with a link to ARCHITECTURE.md. | **2** |

### **Frontend Tasks (FE1–FE4) Sprint 5**

| Who | Frontend Task | Description & folder | Pts |
| :---- | :---- | :---- | ----- |
| **FE1** | **National dashboard with Rwanda choropleth map** **Folder:** app/(government)/dashboard/page.tsx, components/charts/RwandaChoropleth.tsx, lib/api/government.ts | The centerpiece of the government portal. A custom SVG choropleth map of Rwanda showing all 5 provinces, colour-filled by the selected metric (vaccination coverage, ANC attendance, birth registration rate). Clicking a province drills down to district-level. The map SVG paths are sourced from Rwanda's official GeoJSON, converted to SVG paths. A legend bar shows the colour scale. To the right of the map, a stat panel shows the national totals for the selected period. Period and metric selectors above the map control what data is displayed. | **8** |
| **FE2** | **Report generation form and useGenerateReport hook** **Folder:** app/(government)/reports/page.tsx, hooks/useReports.ts, lib/api/government.ts | The report generation form has four controls: report type selector (VACCINATION\_COVERAGE, ANC\_ATTENDANCE, BIRTH\_REGISTRATION, MATERNAL\_HEALTH), period selector (Month, Quarter, or Year with a date picker), scope level selector (NATIONAL, PROVINCE, DISTRICT, SECTOR with a dependent geo selector appearing when DISTRICT or SECTOR is chosen), and a Generate button. useGenerateReport() wraps the POST /api/v1/reports/generate call with react-query's useMutation, showing a progress indicator during generation. | **5** |
| **FE3** | **Report detail page with HMIS push** **Folder:** app/(government)/reports/\[reportId\]/page.tsx, hooks/useReports.ts | Shows the generated report's aggregates as a combination of stat cards and Recharts charts. For VACCINATION\_COVERAGE: a BarChart grouped by vaccine type. For ANC\_ATTENDANCE: a LineChart showing month-by-month attendance. For BIRTH\_REGISTRATION: a BarChart by district. For MATERNAL\_HEALTH: multiple stat cards (maternal mortality proxy, ANC coverage, institutional delivery rate). At the top, a status badge shows NOT\_PUSHED, QUEUED, PUSHED, or FAILED. For MOH\_ADMIN users, a 'Push to HMIS' button appears when status is NOT\_PUSHED or FAILED. | **5** |
| **FE4** | **Sync log monitoring page** **Folder:** app/(government)/sync-log/page.tsx, components/status/SyncStatusIndicator.tsx, lib/api/government.ts | Table of GovSyncLog entries for government portal users. Columns: ID (truncated UUID), created date, target system (NIDA/HMIS/IREMBO), sync type, status badge, retry count, last error message (truncated, expandable). Status badges: PENDING (gray), IN\_FLIGHT (blue with a spinner icon), SUCCEEDED (green), FAILED (amber), DEAD\_LETTER (red with alert icon). DEAD\_LETTER rows have a 'Retry' button (MOH\_ADMIN only) that calls POST .../retry. The table auto-refreshes every 30 seconds using refetchInterval. | **3** |
| **FE2** | **Government user management (MOH\_ADMIN)** **Folder:** app/(government)/users/page.tsx, lib/api/users.ts | List of all DISTRICT\_OFFICER, GOVERNMENT\_ANALYST, and MOH\_ADMIN accounts. For each DISTRICT\_OFFICER, a 'Manage scope' button opens a side panel with a multi-select geo picker: the admin searches for sectors and adds/removes them from the officer's scoped\_geo\_ids. A 'Permissions' section shows toggles for can\_export and can\_push\_hmis. Changes call PATCH /api/v1/government-users/{id}/geo-scope and PATCH .../permissions. The page is only accessible to MOH\_ADMIN (enforced by both middleware and @PreAuthorize). | **5** |
| **FE1** | **District officer dashboard with sector-scoped data** **Folder:** app/(district-officer)/dashboard/page.tsx, app/(district-officer)/facilities/page.tsx | The district officer portal home. A summary table of all facilities in the officer's scoped sectors: facility name, sector, ANC attendance rate, vaccination coverage %, no-show rate all from the facility stats API filtered by the officer's geoScopeIds from the JWT. Clicking a facility row navigates to its detailed stats. Below the table, a heat-map grid shows no-show rates by facility and day of week (rows \= facilities, columns \= weekdays, cell colour \= no-show rate intensity). Both views respect the DISTRICT\_OFFICER's sector scope. | **5** |
| **FE3 \+ FE4** | **WCAG 2.1 AA accessibility audit and fixes** **Folder:** All page components — audit findings fixed in place. Add skipLink to components/layout/Sidebar.tsx | Run axe-core automated audit against all six role portals. Manual keyboard navigation test: every form, table, and interactive element must be reachable and operable with keyboard alone. Screen reader test using NVDA (Windows) and VoiceOver (Mac): all images have meaningful alt text in Kinyarwanda, all form inputs have associated labels, all status badges have text alternatives (not colour alone). Fix all critical and serious violations. Add a skip-to-main-content link on every layout. | **5** |
| **FE1 \+ FE2** | **Production deployment: Vercel \+ Docker \+ monitoring** **Folder:** vercel.json, .github/workflows/deploy.yml, DEPLOYMENT.md, app/src/main/resources/application-prod.yml | Configure Vercel production environment with the production Spring Boot API URL, production NEXTAUTH\_SECRET, and Sentry DSN for frontend error tracking. Configure the Spring Boot Docker image deployment to a production Railway or Render instance with the production PostgreSQL database. Set up Sentry for backend error tracking. Configure Uptime Robot to monitor the health endpoint every 5 minutes. Document the full deployment runbook in DEPLOYMENT.md including rollback procedures. | **3** |

